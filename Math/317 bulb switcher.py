class Solution:
    def bulbSwitch(self, n: int) -> int:
        return int(n**0.5)
    
# The time complexity is O(1) since we are only performing a constant number of operations.
# The space complexity is also O(1) since we are using a constant amount of space