class Solution:
    def toLowerCase(self, s: str) -> str:
        return s.lower()
        
#the time complexity is O(n) where n is the length of the string.
#the space complexity is O(n) because a new string a length n is created.